{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run TaskFlow",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/backend/server.js",
      "cwd": "${workspaceFolder}/backend",
      "preLaunchTask": "Start TaskFlow",
      "console": "integratedTerminal"
    }
  ]
}